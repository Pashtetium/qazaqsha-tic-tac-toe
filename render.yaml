services:
  - type: web
    name: qazaqsha-tic-tac-toe
    runtime: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
